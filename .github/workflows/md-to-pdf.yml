name: Convert Markdown to PDF

on:
  push:
    branches:
      - main

jobs:
  convert-to-pdf:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install pandoc
      run: sudo apt-get install pandoc

    - name: Convert Markdown to PDF
      run: pandoc -s yourfile.md -o output.pdf
